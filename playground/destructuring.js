const person = {
    name: 'Matheus Binotto',
    age: 25,
    location: {
        city: 'Curitiba',
        temp: 1,
    }
};

const name = person.name;
const age = person.age;

console.log(`${name} is ${age}.`)